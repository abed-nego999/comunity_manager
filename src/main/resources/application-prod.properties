# ── Perfil PROD ──────────────────────────────────────────────────────────────
# APIs reales activas — AES_SECRET_KEY y ANTHROPIC_API_KEY obligatorios en entorno

# Consola H2 desactivada
spring.h2.console.enabled=false

# Swagger UI desactivado
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false

# Logs
logging.level.root=WARN
logging.level.com.esteban=INFO

# Meta Graph API — mock desactivado en prod
meta.mock-enabled=false
